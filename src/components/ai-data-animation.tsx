'use client';

import Lottie from 'lottie-react';

const AiDataAnimation = ({className}: {className?: string}) => {
  return <Lottie path="/animations/Ai%20Data.lottie" loop={true} className={className} />;
};

export default AiDataAnimation;
